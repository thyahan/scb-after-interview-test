import React from 'react';
import PropTypes from 'prop-types';

const Daily = props => {
    return (
        <div>
            
        </div>
    );
};

Daily.propTypes = {
    
};

export default Daily;